<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>تفاصيل الفني <%= technician.mainTitle %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
</head>

<body class="m-0 p-0 bg-gray-50" style="font-family: 'Cairo', sans-serif;">
  <!-- header -->
  <%- include("../layouts/header") %>

  <!-- Breadcrumb -->
  <div class="bg-white border-b border-gray-200 py-4 px-6">
    <div class="max-w-6xl mx-auto flex items-center">
      <a href="/" class="text-[#2A4759] hover:text-[#406A84]">
        <i class="fa-solid fa-home text-2xl ml-3"></i>
      </a>
      <i class="fa-solid fa-chevron-left ml-3"></i>
      <span class="font-semibold"><%= technician.mainTitle %></span>
    </div>
  </div>

  <!-- Main Content -->
  <div class="px-8 sm:px-16 mt-12">

    <div class="flex flex-col md:flex-row gap-6 p-4 md:p-8 items-stretch">
      <% 
        let imageName = technician.jobTechnicianPhoto || "default-tech.png";
      %>
      <img src="/uploads/jobs/<%= imageName %>" 
          alt="<%= technician.mainTitle %>" 
          class="rounded-xl w-full md:w-1/2 h-[350px] object-cover" />

      <div class="md:w-1/2 h-full flex flex-col justify-between">
        <h2 class="text-[#2A4759] text-xl font-bold text-right mb-4">
          <%= technician.mainTitle %>
        </h2>
        <p class="text-right leading-relaxed whitespace-pre-line text-sm md:text-base">
          <strong>الوصف:</strong> <%= technician.description %>
        </p>

        <div class="flex gap-4 mt-12">
  <% if (technician.phoneNumber) { %>
    <a href="https://wa.me/<%= technician.phoneNumber %>" target="_blank" rel="noopener noreferrer" class="flex-1">
      <button class="bg-[#2A4759] flex items-center justify-center gap-2 text-white rounded-lg w-full py-2 hover:bg-[#1e3442] transition duration-300">
        <i class="fab fa-whatsapp text-lg"></i>
        <span>إرسال على واتساب</span>
      </button>
    </a>
  <% } %>

 <a href="/technicians/<%= technician._id %>/seeMoreTechnicianNeighborhoods" rel="noopener noreferrer" class="flex-1">
  <button class="bg-white border border-[#2A4759] flex items-center justify-center gap-2 text-[#2A4759] hover:text-white rounded-lg w-full py-2 hover:bg-[#2A4759] transition duration-300">
    <span>عرض الأحياء</span>
  </button>
</a>

</div>

      </div>
    </div>

    <% if (technician.phoneNumber) { %>
    <div class="bg-gray-50 flex flex-col max-w-md mx-auto text-center mt-8">
      <h2 class="text-gray-800 text-xl font-semibold mb-4">اتصل الآن نصلك اينما كنت بالرياض</h2>
      <a href="https://wa.me/<%= technician.phoneNumber %>" target="_blank" class="text-[#2A4759] font-bold underline">
        <%= technician.phoneNumber %>
      </a>
      <a href="https://wa.me/<%= technician.phoneNumber %>" target="_blank"
        class="mt-4 mx-auto w-24 h-24 flex items-center justify-center rounded-full border-2 border-[#2A4759]">
        <div class="bg-[#2A4759] w-16 h-16 rounded-full flex items-center justify-center">
          <i class="fab fa-whatsapp text-3xl text-white"></i>
        </div>
      </a>
    </div>
    <% } %>

    <% if (technician.jobName) { %>
    <div class="px-8 sm:px-16 mt-8 mb-8">
      <p class="text-right leading-relaxed whitespace-pre-line text-sm md:text-base">
        <%= technician.jobName.mainDescription %>
      </p>
      <p class="text-right leading-relaxed whitespace-pre-line text-sm md:text-base mt-2">
        <%= technician.jobName.subDescription %>
      </p>
    </div>
    <% } %>

  </div>

  <!-- footer -->
  <%- include("../layouts/footer") %>
</body>
</html>
