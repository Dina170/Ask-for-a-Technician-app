<!DOCTYPE html>
<html>
<head>
  <title><%= neighborhood.name %> Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 700px;
      margin: 20px auto;
      padding: 10px;
    }
    img {
      max-width: 100%;
      border-radius: 8px;
      margin-bottom: 15px;
    }
    h1, h2 {
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <h1><%= neighborhood.name %></h1>

  <!-- Job Image (prefer job.jobImage over technician.jobName.jobImage) -->
  <% if (job && job.jobImage) { %>
    <img src="/uploads/jobs/<%= job.jobImage %>" alt="Job Image" />
  <% } else if (technician.jobName && technician.jobName.jobImage) { %>
    <img src="/uploads/jobs/<%= technician.jobName.jobImage %>" alt="Job Image" />
  <% } %>

  <!-- Main job description (prefer job.mainDescription) -->
  <p><%= job ? job.mainDescription : (technician.jobName ? technician.jobName.mainDescription : '') %></p>

  <!-- Technician details -->
  <h2>Technician: <%= technician.mainTitle %></h2>
  <p>Phone: <a href="tel:<%= technician.phoneNumber %>"><%= technician.phoneNumber %></a></p>
  <p>Description: <%= technician.description %></p>

  <!-- Sub job description (prefer job.subDescription) -->
  <p><%= job ? job.subDescription : (technician.jobName ? technician.jobName.subDescription : '') %></p>

  <a href="/technicians/<%= technician._id %>/neighborhoods">Back to Neighborhoods</a>
</body>
</html>

